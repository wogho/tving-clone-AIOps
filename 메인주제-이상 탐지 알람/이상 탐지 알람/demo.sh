#!/bin/bash
# ==============================================================================
# TVING AIOps 이상 탐지 알람 시연 실행 쉘 스크립트
# ==============================================================================

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
python3 "$DIR/demo_anomaly_alarm.py" "$@"
